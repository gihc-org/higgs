apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: higgs
resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - pvc.yaml
  - ipfs.yaml
configMapGenerator:
  - name: higgs-feed
    files:
      - feed.xml
      - logo.svg
      - logo.png
      - nginx/default.conf
